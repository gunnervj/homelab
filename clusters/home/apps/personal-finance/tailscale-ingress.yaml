apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finance
  namespace: personal-finance
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - finance
  defaultBackend:
    service:
      name: frontend
      port:
        number: 3000
