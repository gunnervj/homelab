apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: langfuse
  namespace: langfuse
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - langfuse
  defaultBackend:
    service:
      name: langfuse
      port:
        number: 3000
