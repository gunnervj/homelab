apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: affine
  namespace: affine
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - affine
  defaultBackend:
    service:
      name: affine
      port:
        number: 3010
